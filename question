1. test_login1, 为什么视频里主程序里直接运行可以出打印结果，但我实际操作需要调用函数
2. read_data, 返回出来没有想要的函数
3. setup(),teardown()也没有预期结果
4.     def hello(self,name,age):
        #self.name = name
        #self.age = age 这两行有什么作用？
        return (f'{name} is {age} years old')